export interface AnimeSearchResult {
  title: string
  id: string
  image: string
}

export interface Episode {
  number: number
  url: string
}

export interface Stream {
  url: string
  quality: string
}